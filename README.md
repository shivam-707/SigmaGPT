# SigmaGPT

This repository contains the SigmaGPT project workspace.
The files `.env` and `server.js` are intentionally excluded from version control.
All remaining files are included in the repository history.
